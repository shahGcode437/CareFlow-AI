# CareFlow AI frontend  local environment template.
# Copy this file to .env for local development. Never commit the real .env.

# Base URL of the FastAPI backend. Used by the API client (added in
# Phase 8.2). Include the scheme, host, and port  no trailing slash.
VITE_API_BASE_URL=http://localhost:8000
