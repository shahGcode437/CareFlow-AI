# CareFlow AI backend dependencies.
# Excel/pandas/openpyxl and any agent/LLM SDK are added in later phases
# per the Claude Implementation Master Guide's phase plan.

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1

# Added in Phase 2 — required to verify the Pydantic schema layer
# (Implementation Guide Phase 2 gate: "Schema validation tests pass").
pytest==8.3.4

# Added in Phase 3 — Excel repository layer. pandas was pre-approved but
# is not imported anywhere in this phase's implementation (openpyxl's
# row/cell-level API was a better fit for the write-lock + update-by-key
# pattern), so it is intentionally not added yet to avoid an unused
# dependency.
openpyxl==3.1.5

# Added in Phase 6 — required by FastAPI's TestClient (fastapi.testclient)
# used in tests/test_routes.py. Test-only; not used at application runtime.
httpx==0.28.1

# Added in Phase 8.8.1 — dependencies for the Knowledge/RAG capability
# (Master Spec SYS-02 / UC-01, UC-02 / FR-003). Chosen per the Phase 8.8
# inspection report: numpy for an in-memory cosine vector store,
# fastembed for local ONNX-runtime embeddings (no torch, no external
# API key), and pyyaml to load the doctors.yaml knowledge source. No
# LangChain / FAISS / Chroma / sentence-transformers / torch / OpenAI
# SDK are introduced — the codebase pattern is deliberately framework-
# independent (see app/tools/__init__.py).
numpy==2.1.3
fastembed==0.4.2
pyyaml==6.0.2
